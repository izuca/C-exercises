#include <stdio.h>

int main()
{
    int l,c,camisa,extremidade,pe,gorro;
    camisa = 1111;
    extremidade = 0;
    pe = 8;
    gorro = 4;

    scanf("%i %i",&l,&c);

    int matriz[l][c];

    for(int linha = 0; linha < l;linha++)
    {
        for(int coluna = 0; coluna < c;coluna++)
            scanf("%i %i",&matriz[linha][coluna]);
    }

    for(int linha = 0; linha < l;linha++)
    {
        for(int coluna = 0; coluna < c;coluna++)
        {
            if()
        }
    }

    return 0;
}
